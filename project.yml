parameters: {}
environment: {
  LOG_DESTINATIONS: '${LOG_DESTINATIONS}',
  LOGLEVEL: '${LOGLEVEL}',
  HEARTBEAT_TOKEN: '${HEARTBEAT_TOKEN}'
}
packages:
  - name: p
    environment: {}
    parameters: {}
    actions:
      - name: r
        main: ''
        runtime: 'go:default'
        environment: {}
        parameters: {}
